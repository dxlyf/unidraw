/**
 * 混合模式与材质选项测试：
 * - `MaterialOptions.blend` / `depthWrite` 是否真的进到管线描述里；
 * - `isTransparent` 的判定（`alphaBlend` 或 `blend`）；
 * - `BLEND_PRESETS` 预设的完整性与回退；
 * - `SceneRenderer` 的半透明排序：不透明先画、半透明按距离**远→近**。
 */
export {};
//# sourceMappingURL=material-blend.test.d.ts.map